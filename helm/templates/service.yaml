apiVersion: v1
kind: Service
metadata:
  name: {{ include "bisc-taalhuizen.fullname" . }}
  labels:
    {{- include "bisc-taalhuizen.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bisc-taalhuizen.selectorLabels" . | nindent 4 }}
