query findEmployeesByPersonId($personId: String) {
    employees(person: $personId) {
        edges {
            node {
                id
                person
                organization
            }
        }
    }
}
