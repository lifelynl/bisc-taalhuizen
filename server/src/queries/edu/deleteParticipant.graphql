mutation deleteParticipant($input: deleteParticipantInput!) {
    deleteParticipant(input: $input) {
        participant {
            id
            program {
                id
                name
            }
        }
    }
}
