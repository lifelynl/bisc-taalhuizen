query findParticipantById($id: ID!) {
    participant(id: $id) {
        id
        person
        status
        dateCreated
        referredBy
        program {
            id
            name
        }
    }
}
