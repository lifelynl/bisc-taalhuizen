mutation createPerson($input: createPersonInput!) {
    createPerson(input: $input) {
        person {
            id
            givenName
            additionalName
            familyName
        }
    }
}
