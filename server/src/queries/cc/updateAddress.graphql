mutation updateAddress($input: updateAddressInput!) {
    updateAddress(input: $input) {
        address {
            id
            street
            houseNumber
            houseNumberSuffix
            postalCode
            locality
        }
    }
}
