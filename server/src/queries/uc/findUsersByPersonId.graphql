query findUsersByPersonId($personId: String!) {
    users(person: $personId) {
        edges {
            node {
                id
                username
                dateCreated
                dateModified
                userGroups {
                    edges {
                        node {
                            id
                            name
                        }
                    }
                }
            }
        }
    }
}
