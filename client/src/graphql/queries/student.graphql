query student($studentId: String!) {
    student(studentId: $studentId) {
        id
        dateCreated
        status
        givenName
        additionalName
        familyName
        memo
        registrar {
            id
            organisationName
            givenName
            additionalName
            familyName
            email
            telephone
        }
    }
}
