query document($document: String!) {
    document(document: $document) {
        id
        createdAt
        updatedAt
        file {
            name
            extension
            mimeType
            size
            base64
        }
    }
}
