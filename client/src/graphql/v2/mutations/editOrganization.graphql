mutation editOrganization($input: EditOrganizationInputType!) {
	editOrganization(input: $input) {
		id
		name
		description
		type
		employees {
			id
		}
		address {
			id
		}
    postalCodes{
      id
      code
    }
	}
}
